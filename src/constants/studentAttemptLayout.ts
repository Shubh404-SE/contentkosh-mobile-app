/** Min width (dp) to show question list beside the main panel (mirrors web lg breakpoint). */
export const STUDENT_ATTEMPT_SIDE_NAV_BREAKPOINT = 900;

/** Width of the question navigator column on wide layouts. */
export const STUDENT_ATTEMPT_NAVIGATOR_WIDTH = 300;
